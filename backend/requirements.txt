# Web framework 
Flask==3.1.1
gunicorn==23.0.0         # production WSGI server

# Database 
Flask-SQLAlchemy==3.1.1  # ORM integration
Flask-Migrate==4.1.0     # Alembic-based schema migrations
psycopg2-binary==2.9.10  # PostgreSQL driver (binary = no native build deps)

# CORS 
Flask-Cors==5.0.1        # Cross-Origin Resource Sharing headers

# Rate limiting backend 
redis==5.2.1             # Redis client — backs the sliding-window rate limiter

# URL safety
requests==2.32.3         # HTTP client — used to call Google Safe Browsing

# Utilities 
python-dotenv==1.1.0     # Load .env files in development
